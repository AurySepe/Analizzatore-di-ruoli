apiVersion: apps/v1
kind: Deployment
metadata:
  name: relay-curriculum
  labels:
    app: relay-curriculum
    app.kubernetes.io/name: relay-curriculum
    app.kubernetes.io/component: relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: relay-curriculum
  template:
    metadata:
      labels:
        app: relay-curriculum
    spec:
      containers:
        - name: relay-curriculum
          image: analizzatore-registry.localhost:5000/relay-curriculum:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
