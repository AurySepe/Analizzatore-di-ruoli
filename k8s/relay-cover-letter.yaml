apiVersion: apps/v1
kind: Deployment
metadata:
  name: relay-cover-letter
  labels:
    app: relay-cover-letter
    app.kubernetes.io/name: relay-cover-letter
    app.kubernetes.io/component: relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: relay-cover-letter
  template:
    metadata:
      labels:
        app: relay-cover-letter
    spec:
      containers:
        - name: relay-cover-letter
          image: analizzatore-registry.localhost:5000/relay-cover-letter:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
